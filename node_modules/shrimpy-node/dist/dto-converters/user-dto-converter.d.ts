import { IUserDto } from "../dtos";
import { IDtoConverter } from "../interfaces";
import { IUser } from "../models";
export declare class UserDtoConverter implements IDtoConverter<IUserDto, IUser> {
    private _dateDtoConverter;
    convertFromDto(dto: IUserDto): IUser;
    convertToDto(model: IUser): IUserDto;
}
