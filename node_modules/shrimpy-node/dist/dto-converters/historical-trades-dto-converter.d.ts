import { IDtoConverter } from "../interfaces";
import { IHistoricalTradeDto } from "../dtos";
import { IHistoricalTrade } from "../models";
export declare class TakerSideDtoConverter implements IDtoConverter<string, "buyer" | "seller" | "unknown"> {
    convertFromDto(dto: string): "buyer" | "seller" | "unknown";
    convertToDto(model: "buyer" | "seller" | "unknown"): string;
}
export declare class HistoricalTradesDtoConverter implements IDtoConverter<IHistoricalTradeDto[], IHistoricalTrade[]> {
    private _dateDtoConverter;
    private _decimalDtoConverter;
    private _takerSideDtoConverter;
    convertFromDto(dto: IHistoricalTradeDto[]): IHistoricalTrade[];
    convertToDto(model: IHistoricalTrade[]): IHistoricalTradeDto[];
}
