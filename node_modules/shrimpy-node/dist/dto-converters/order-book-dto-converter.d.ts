import { IDtoConverter } from "../interfaces";
import { IOrderBookDto } from "../dtos";
import { IOrderBook } from "../models";
export declare class OrderBookDtoConverter implements IDtoConverter<IOrderBookDto, IOrderBook> {
    private _orderBookItemDtoConverter;
    convertFromDto(dto: IOrderBookDto): IOrderBook;
    convertToDto(model: IOrderBook): IOrderBookDto;
}
