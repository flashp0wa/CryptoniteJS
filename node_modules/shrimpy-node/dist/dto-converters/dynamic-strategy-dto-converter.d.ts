import { IDtoConverter } from "../interfaces";
import { IDynamicStrategyDto } from "../dtos";
import { IDynamicStrategy } from "../models";
export declare class DynamicStrategyDtoConverter implements IDtoConverter<IDynamicStrategyDto, IDynamicStrategy> {
    private _decimalDtoConveter;
    convertFromDto(dto: IDynamicStrategyDto): IDynamicStrategy;
    convertToDto(model: IDynamicStrategy): IDynamicStrategyDto;
}
