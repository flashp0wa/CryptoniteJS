import { IDtoConverter } from "../interfaces";
import { IPredictionsDto } from "../dtos";
import { IPredictions } from "../models";
export declare class PredictionsDtoConverter implements IDtoConverter<IPredictionsDto, IPredictions> {
    private _predictionItemDtoConverter;
    convertFromDto(dto: IPredictionsDto): IPredictions;
    convertToDto(model: IPredictions): IPredictionsDto;
}
