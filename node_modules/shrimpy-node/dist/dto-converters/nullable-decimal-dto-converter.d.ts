import Decimal from "decimal.js";
import { IDtoConverter } from "../interfaces";
export declare class NullableDecimalDtoConverter implements IDtoConverter<string | null, Decimal | null> {
    private _decimalDtoConverter;
    convertFromDto(dto: string | null): Decimal | null;
    convertToDto(model: Decimal | null): string | null;
}
