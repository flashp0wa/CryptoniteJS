import { IDtoConverter } from "../interfaces";
import Decimal from "decimal.js";
export declare class DecimalDtoConverter implements IDtoConverter<string, Decimal> {
    convertFromDto(dto: string): Decimal;
    convertToDto(model: Decimal): string;
}
