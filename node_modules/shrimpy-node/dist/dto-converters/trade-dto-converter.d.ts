import { IDtoConverter } from "../interfaces";
import { ITradeDto } from "../dtos";
import { ITrade } from "../models";
export declare class TradeDtoConverter implements IDtoConverter<ITradeDto, ITrade> {
    private _decimalDtoConverter;
    convertFromDto(dto: ITradeDto): ITrade;
    convertToDto(model: ITrade): ITradeDto;
}
