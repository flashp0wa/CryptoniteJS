import { IDtoConverter } from "../interfaces";
import { IPredictionItemDto } from "../dtos";
import { IPredictionItem } from "../models";
export declare class PredictionItemDtoConverter implements IDtoConverter<IPredictionItemDto, IPredictionItem> {
    convertFromDto(dto: IPredictionItemDto): IPredictionItem;
    convertToDto(model: IPredictionItem): IPredictionItemDto;
}
