import { IDtoConverter } from "../interfaces";
import { IStrategyDto } from "../dtos";
import { IStrategy } from "../models";
export declare class StrategyDtoConverter implements IDtoConverter<IStrategyDto, IStrategy> {
    private _dynamicStrategyDtoConverter;
    private _staticStrategyDtoConverter;
    convertFromDto(dto: IStrategyDto): IStrategy;
    convertToDto(model: IStrategy): IStrategyDto;
}
