import { IDtoConverter } from "../interfaces";
export declare class DateDtoConverter implements IDtoConverter<string, Date> {
    convertFromDto(dto: string): Date;
    convertToDto(model: Date): string;
}
