export declare class AuthenticationProvider {
    readonly publicKey: string;
    private _privateKey;
    constructor(publicKey: string, privateKey: string);
    sign(prehashString: string): string;
}
